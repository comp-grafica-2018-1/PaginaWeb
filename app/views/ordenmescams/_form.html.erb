<%= form_with(model: ordenmescam, local: true) do |form| %>
  <% if ordenmescam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ordenmescam.errors.count, "error") %> prohibited this ordenmescam from being saved:</h2>

      <ul>
      <% ordenmescam.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
