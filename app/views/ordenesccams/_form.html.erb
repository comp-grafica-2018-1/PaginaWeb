<%= form_with(model: ordenesccam, local: true) do |form| %>
  <% if ordenesccam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ordenesccam.errors.count, "error") %> prohibited this ordenesccam from being saved:</h2>

      <ul>
      <% ordenesccam.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
